<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liyafeng.view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".slidingmenu.SlidingMenuActivity" />
        <activity android:name=".redpoint.RedPointActivity" />
        <activity android:name=".drag.DragActivity" />
        <activity android:name=".viewpager.ViewPagerActivity" />
        <activity android:name=".viewpager.CardPagerActivity" />
        <activity android:name=".swipecard.SwipeCardActivity" />
        <activity android:name=".fragment.CustomFragmentActivity" />
        <activity android:name=".listview.ListViewActivity" />
        <activity android:name=".scrollview.ScrollActivity" />
        <activity android:name=".scroller.ScrollerActivity" />
        <activity android:name=".recycleview.RecyclerViewActivity"></activity>
    </application>

</manifest>